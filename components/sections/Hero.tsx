import Container from "../Container";
import NavBar from "../ui/NavBar";

const Hero = () => {
  return (
    <div className="bg-bites-green h-[100vh]">
      <Container>
        <NavBar />
      </Container>
    </div>
  );
};

export default Hero;
